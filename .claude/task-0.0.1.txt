你是技术专家。先读 .claude/CLAUDE.md、docs/architecture.md、docs/iterations/0.0.1.md 和相关 ADR（如有）。

本项目是合同管理系统 MVP，迭代 0.0.1。技术栈：Python FastAPI + SQLAlchemy + SQLite 后端，Jinja2 SSR 前端。部署到 http://120.24.117.67/projects/contract-manager-eval/。

任务按 docs/iterations/0.0.1.md 中 T1-T8 拆解执行：

T1: 项目骨架 + 配置 + 依赖
- requirements.txt (已有)
- .env.example (已有)
- app/__init__.py, app/config.py
- 安装依赖 (pip install -r requirements.txt)

T2: 数据库层
- app/database.py, app/models.py (User, Contract, Attachment, AuditLog)
- scripts/init_db.py (建表 + 3个演示账号种子数据)
- 运行验证

T3: 认证模块
- app/auth.py (bcrypt + JWT), app/dependencies.py, app/middleware.py (Cache-Control)

T4: API 路由
- app/schemas.py, app/utils.py
- app/routers/auth.py, users.py, contracts.py, attachments.py
- app/main.py (组装应用 + 前端页面路由 + healthz)

T5: 前端模板 + 静态资源
- static/css/style.css, static/js/app.js
- templates/ (base.html, login.html, contracts/*, users/*)
- 页面路由配置

T6: 测试
- tests/conftest.py, test_auth.py, test_users.py, test_contracts.py, test_attachments.py
- 运行全部测试确保通过

T7: 文档
- 填写 docs/architecture.md, docs/runbook.md
- docs/decisions/ 下 ADR

T8: 终验
- 运行完整测试套件
- 保存输出到 evidence/claude/
- 检查 CLAUDE.md 约束
- 报告自测完成，申请 Hermes 验收

每个 task 完成后 git add + commit（小步提交，遵守 .cursorrules 的 Agent-* trailer）。完成后报告自测结果和测试命令输出。

注意：
- 所有 HTML 响应头必须包含 Cache-Control: no-cache
- 静态资源路径使用 /projects/contract-manager-eval/static/ 前缀
- 附件校验：扩展名 + magic bytes，≤10MB
- 演示账号密码用 bcrypt 哈希
- /healthz 端点返回 200